server:
  port: 7778

spring:
  application:
    name: users-server
  profiles:
    active: development
  jpa:
    hibernate:
      ddl-auto: update

users:
  datasource:
    url: jdbc:postgresql://users_db:5432/usersDb
    username: usersUser
    password: usersPasswd
    driver-class-name: org.postgresql.Driver
